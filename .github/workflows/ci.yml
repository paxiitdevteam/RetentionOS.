name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  backend-test:
    name: Backend Tests
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: retentionos
          POSTGRES_PASSWORD: password
          POSTGRES_DB: retentionos_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package.json
      
      - name: Install dependencies
        working-directory: ./backend
        run: npm ci
      
      - name: Run linter
        working-directory: ./backend
        run: npm run lint || true
      
      - name: Run tests
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql://retentionos:password@localhost:5432/retentionos_test
          REDIS_URL: redis://localhost:6379
          NODE_ENV: test
        run: npm test || true
      
      - name: Build
        working-directory: ./backend
        run: npm run build

  dashboard-test:
    name: Dashboard Tests
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/dashboard/package.json
      
      - name: Install dependencies
        working-directory: ./frontend/dashboard
        run: npm ci
      
      - name: Run linter
        working-directory: ./frontend/dashboard
        run: npm run lint || true
      
      - name: Build
        working-directory: ./frontend/dashboard
        run: npm run build

  widget-build:
    name: Widget Build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/widget/package.json
      
      - name: Install dependencies
        working-directory: ./frontend/widget
        run: npm ci
      
      - name: Build widget
        working-directory: ./frontend/widget
        run: npm run build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: widget-build
          path: frontend/widget/build/
          retention-days: 7

  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Check file structure
        run: |
          test -f backend/package.json || exit 1
          test -f frontend/dashboard/package.json || exit 1
          test -f frontend/widget/package.json || exit 1
          test -f infra/docker-compose.yml || exit 1
          echo "✅ File structure validated"

